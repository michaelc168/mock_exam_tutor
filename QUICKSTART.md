# 🚀 快速開始

## 一鍵啟動

```bash
./start-dev.sh
```

就這麼簡單！腳本會自動：
- ✅ 建立 Python 虛擬環境
- ✅ 安裝所有依賴
- ✅ 啟動後端 (Port 8000)
- ✅ 啟動前端 (Port 3000)
- ✅ 自動開啟瀏覽器

## 停止服務

```bash
./stop-dev.sh
```

## 系統測試

```bash
./test-system.sh
```

檢查所有組件是否正常。

---

## 手動啟動（進階）

### 1. 啟動後端

```bash
cd backend
source venv/bin/activate
uvicorn main:app --reload --port 8000
```

### 2. 啟動前端

```bash
cd frontend
npm run dev
# 或
pnpm dev
```

---

## 訪問服務

| 服務 | URL | 說明 |
|------|-----|------|
| 前端 | http://localhost:3000 | 使用者介面 |
| 後端 API | http://localhost:8000 | API 伺服器 |
| API 文檔 | http://localhost:8000/docs | Swagger UI |
| API 文檔 (替代) | http://localhost:8000/redoc | ReDoc UI |

---

## 使用流程

### 1️⃣ 選擇科目出題

在首頁點擊任一卡片：
- **國語科** → 20 題
- **英語科** → 20 題
- **數學科** → 40 題
- **綜合考卷** → 80 題（20+20+40）

### 2️⃣ 查看考卷

點擊右上角「我的考卷」查看所有已生成的試卷

### 3️⃣ 下載考卷

點擊「下載」按鈕，優先下載 PDF 版本

---

## 疑難排解

### 問題：前端無法連接後端

**檢查**：
```bash
curl http://localhost:8000
# 應該返回: {"status":"ok",...}
```

如果沒有回應，重新啟動後端。

### 問題：Port 已被佔用

**後端 (8000)**：
```bash
lsof -ti:8000 | xargs kill -9
```

**前端 (3000)**：
```bash
lsof -ti:3000 | xargs kill -9
```

### 問題：PDF 生成失敗

確保 Node.js 依賴已安裝：
```bash
npm install
```

---

## 目錄結構

```
mock_exam_tutor/
├── frontend/              # Next.js 前端
│   ├── app/
│   │   ├── page.tsx      # 首頁
│   │   └── exams/        # 考卷列表頁
│   ├── components/       # UI 元件
│   └── lib/
│       └── api.ts        # API 客戶端
├── backend/              # FastAPI 後端
│   ├── main.py          # API 伺服器
│   ├── venv/            # Python 虛擬環境
│   └── requirements.txt
├── exams/
│   ├── bank/            # 題庫
│   └── generated/       # 生成的考卷
├── scripts/
│   └── convert-to-pdf.js # PDF 轉換
├── start-dev.sh         # 啟動腳本
├── stop-dev.sh          # 停止腳本
└── test-system.sh       # 系統測試
```

---

## 下一步

查看完整文檔：
- [README-UI.md](./README-UI.md) - 詳細說明
- [API 文檔](http://localhost:8000/docs) - 啟動後查看

**享受 AI 出題吧！** 🎉
