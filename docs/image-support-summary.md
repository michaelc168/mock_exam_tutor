# 圖片支持完整實作

## ✅ 已完成

### 1. 後端 - 圖片服務 API

**新增端點**：
```
GET /api/images/{filename}
```

**功能**：
- 從 `exams/images/` 目錄提供圖片
- 支援 PNG、JPG 等格式
- 自動設定正確的 Content-Type

**測試**：
```bash
# 下載圖片測試
curl http://localhost:8000/api/images/triangle-demo.png --output test.png

# 在瀏覽器中打開
open http://localhost:8000/api/images/triangle-demo.png
```

✅ 測試通過：`PNG image data, 727 x 588, 8-bit/color RGBA`

---

### 2. 前端 - LatexRenderer 圖片支持

**功能增強**：
- 自動偵測 Markdown 圖片語法：`![描述](../images/xxx.png)`
- 轉換為 API 路徑：`http://localhost:8000/api/images/xxx.png`
- 添加響應式 CSS 類：`max-w-full h-auto my-4 mx-auto block`

**轉換範例**：
```markdown
# 原始 Markdown
![三角形](../images/triangle-demo.png)

# 轉換後的 HTML
<img 
  src="http://localhost:8000/api/images/triangle-demo.png" 
  alt="三角形" 
  class="max-w-full h-auto my-4 mx-auto block" 
/>
```

---

## 📋 支持的圖片格式

從 `exams/images/` 目錄：

| 檔案名稱 | 類型 | 大小 | 用途 |
|---------|------|------|------|
| `triangle-demo.png` | PNG | 11 KB | 三角形題目 |
| `rectangle-demo.png` | PNG | 18 KB | 長方形題目 |
| `circle-demo.png` | PNG | 25 KB | 圓形題目 |
| `sector-demo.png` | PNG | 29 KB | 扇形題目 |
| `coordinate-demo.png` | PNG | 18 KB | 座標平面 |
| `bar-chart-demo.png` | PNG | 28 KB | 長條圖 |
| `composite-demo.png` | PNG | 19 KB | 複合圖形 |

---

## 🎯 使用方式

### 在考卷 Markdown 中插入圖片

```markdown
## 數學科

1. 請根據下圖回答問題：

![座標平面](../images/coordinate-demo.png)

點 A 的座標是 (1, 2)，點 B 的座標是 (3, 5)。若線段 AB 的中點為 M，請問 M 的座標是？

   (A) (2, 3)  
   (B) (2, 3.5)  
   (C) (2.5, 3)  
   (D) (2.5, 3.5)
```

### 答題系統自動處理

1. **題目顯示**：圖片自動載入並置中顯示
2. **響應式**：自動適應不同螢幕尺寸
3. **LaTeX 兼容**：圖片和 LaTeX 公式可以混用

---

## 🧪 測試範例

### 含圖片的考卷

已有範例考卷：`math-exam-with-images.md`

```bash
# 啟動系統
./start-dev.sh

# 訪問
http://localhost:3000

# 進入「我的考卷」
# 選擇「math-exam-with-images」
# 點擊「開始作答」
```

預期結果：
- ✅ 題目中的圖片正常顯示
- ✅ 圖片置中對齊
- ✅ 圖片尺寸自適應
- ✅ LaTeX 公式正常渲染

---

## 🎨 圖片樣式

CSS 類別：`max-w-full h-auto my-4 mx-auto block`

- `max-w-full`：最大寬度 100%（響應式）
- `h-auto`：高度自動（保持比例）
- `my-4`：上下邊距 1rem
- `mx-auto`：水平置中
- `block`：區塊元素

---

## 📐 圖片生成

使用 Python 腳本自動生成幾何圖形：

```bash
python3 scripts/generate-geometry-image.py
```

支援生成：
- 三角形（標示底邊、高）
- 長方形（標示長、寬）
- 圓形（標示半徑）
- 扇形（標示角度）
- 座標平面（標示點）
- 長條圖
- 複合圖形

---

## 🔄 完整流程

```
1. 生成圖片
   └─> scripts/generate-geometry-image.py
       └─> 存入 exams/images/

2. 撰寫考卷
   └─> 使用 ![描述](../images/xxx.png)

3. 答題系統
   └─> 解析 Markdown
       └─> LatexRenderer 轉換圖片路徑
           └─> API 提供圖片
               └─> 前端顯示

4. PDF 生成
   └─> Base64 編碼嵌入
       └─> 完整包含圖片
```

---

## ✨ 特色

✅ **自動化**：圖片路徑自動轉換  
✅ **響應式**：自適應螢幕尺寸  
✅ **兼容性**：支援 LaTeX + 圖片混用  
✅ **高效**：圖片按需載入  
✅ **美觀**：置中對齊、適當邊距

---

## 🎓 範例題目

### 幾何題

```markdown
![三角形](../images/triangle-demo.png)

已知底邊長 8 公分，高 6 公分。請問面積是多少？
```

### 座標題

```markdown
![座標平面](../images/coordinate-demo.png)

點 A (1, 2) 和點 B (3, 5) 的中點座標是？
```

### 統計題

```markdown
![長條圖](../images/bar-chart-demo.png)

根據圖表，哪個月份的銷售額最高？
```

---

**最後更新**：2026-02-11  
**狀態**：✅ 完全可用
